# =====================================================
# 三高知识智能问答平台 — 依赖清单
# =====================================================
# 安装方法（任选其一）：
#   pip install -r requirements.txt
# 如果国内网络有限，可加镜像：
#   pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
# =====================================================

# Web UI
streamlit>=1.32.0

# 知识图谱可视化
networkx>=3.2
pyvis>=0.3.2

# 数值与基础工具
numpy>=1.24,<3.0
pandas>=2.0,<2.4
tiktoken>=0.7.0
python-dotenv>=1.0.0
pydantic>=2.0
tenacity>=8.0
packaging>=22.0
configparser>=5.0
setuptools>=64
xlsxwriter>=3.1
pypinyin>=0.50.0
matplotlib>=3.7
seaborn>=0.12

# OpenAI 兼容客户端（硅基流动接口走这一套）
openai>=1.30.0
aiohttp>=3.9

# LightRAG 自身的若干必备依赖（仓库内自带源码，无需 pip install lightrag-hku）
json_repair>=0.30
nano-vectordb>=0.0.4
pipmaster>=0.4.0

# Google 兼容（LightRAG 的 llm 子模块在 import 时会用到，安装上避免报错）
google-api-core>=2.0.0,<3.0.0
google-genai>=1.0.0,<2.0.0
